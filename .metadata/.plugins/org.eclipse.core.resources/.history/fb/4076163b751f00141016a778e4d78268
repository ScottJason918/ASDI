//requiring file
var req = require("data");
var data = req.litNot;
var data2 = req.dotNot;
//main Window
var mainWindow = Ti.UI.createWindow({
	title : "My Games",
	backgroundColor : "#FFF"
});
//creating table
var table = Ti.UI.createTableView({
});
//alert from object
var anAlert = function(){
	this.info;
};

//making table view rows from literal notation
for(var i = 0; i < data.length; i++){
	var aRow = Ti.UI.createTableViewRow({
		title : data[i].name,
		desc : data[i].type,
		hasChild: true
	});
	table.add(aRow);
	//aRow.addEventListener("click", anAlert);
};

//making table view rows from dot notation
// for(var i = 0; i < data2.length; i++){
	// var aRow = Ti.UI.createTableViewRow({
		// title : data2[i].name,
		// desc : data2[i].type,
		// hasChild: true,
	// });
	// table.add(aRow);
	// aRow.addEventListener("click", anAlert);
// };
// 
// ///Testing
//console.log(data[2].name);
// //console.log(req.dotNot);
for(var i = 0; i < data.length; i++){
	console.log(data[i].name);
};

///Opening Window
mainWindow.add(table);
mainWindow.open();
